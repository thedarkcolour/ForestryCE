my findings:
- GOOD: You correctly fixed "client GUI crash + no highlight after reload"
- GOOD: You correctly fixed "The multiblock is too small" message for when the Alveary was incomplete.
- GOOD: Destroying the anchor block and replacing it does not wipe the multiblock's contents. This partially fixes the re-anchor bug.
- PROBLEM: Destroying the anchor block and several other parts of the multiblock, then replacing those parts DOES wipe the inventory. This could mean either the re-anchor logic isn't properly working, or it's a side effect of the next problem (items not dropping).
- PROBLEM: Destroying every block in the multiblock does not drop the contents of the Farm/Alveary. Having this behavior would make the first problem acceptable. removing one block and placing it back without spilling contents is good (makes swapping parts less annoying), but removing several at once might make it impossible for the multiblock to re-anchor (ex. anchor position is cut off from rest of multiblock, then destroyed). in that case, the multiblock dropping its contents before all of its blocks are destroyed is acceptable, because nothing was lost. however, since nothing is dropped, it might mean players accidentally wipe the inventory contents if swapping multiple parts at once.
- QUESTION: Using the /forestry multiblock debug reports no issues, and inventory remains in tact. However, the command doesn't actually unload or load chunks, so i'm unsure if it's good of an approximation of real behavior. Can you make a separate version of the command I can test against the old Multiblock system later on? If it successfully corrupts those multiblocks, then I will accept it as an approximation and useful benchmark.
- QUESTION: Can you make the spectacles always highlight "anchor" positions even if they're not part of a formed multiblock? Maybe use a flashing white outline for the unformed anchors (you can use HSL + sin like the existing rainbow color).
